import { environment } from 'src/environments/environment';

export const CONFIG = {
    imagePath: environment.APIEndPoint+'public/images/',
    adminPath: 'localhost',
    userLoggedPath: 'localhost'
};
